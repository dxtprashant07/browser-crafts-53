# Normalize line endings to LF in the repo (prevents CRLF lint failures in CI).
* text=auto eol=lf

# Binary assets — never normalize.
*.ico binary
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.gif binary
*.woff binary
*.woff2 binary
*.ttf binary
*.wasm binary
*.pdf binary
